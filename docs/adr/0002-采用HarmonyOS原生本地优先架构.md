---
status: accepted
---

# 采用 HarmonyOS 原生本地优先架构

第一版主要运行在一台华为 MatePad 11.5 上，采用 ArkTS、ArkUI Stage 模型和 ArkData 本地关系型数据库，核心流程完全离线运行，不部署服务端、同步服务或其他中间件。该选择以家庭日常使用和数据可靠性为优先，接受 HarmonyOS 专用开发、签名安装及跨平台复用成本；数据通过版本化 JSON 导入导出保护，只有未来出现明确的多设备共享需求时才增加远端同步架构。
