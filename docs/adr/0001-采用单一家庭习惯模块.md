---
status: accepted
---

# 采用单一家庭习惯 Module

第一版采用一个深的家庭习惯 Module，通过 `openSession`、`execute` 和 `inspect` 三个入口统一承载会话、权限、任务、目标、打卡、清算、反馈语义和备份规则，并独占领域数据写入。这样可以用最少的 Interface 隐藏每日清算、撤销和历史重算的复杂度；代价是 Module 内部职责较多，只有在出现第二种真实 Adapter 或 Interface 明显膨胀时才提取新的外部 Seam。
